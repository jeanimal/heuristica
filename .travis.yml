# Configures the travis continueous build.

language: R
sudo: required
# TODO(Jean): Re-add warnings_are_errors after line length fixed.
# warnings_are_errors: true

r_packages:
  - roxygen2

after_failure: "cat /home/travis/build/jeanimal/heuristica/heuristica.Rcheck/00check.log"
